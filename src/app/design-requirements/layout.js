export const metadata = {
  title: "Design Requirements | Guidelines for Success | Aone",
  description: "Review our design standard and technical requirements. Ensuring high-performance results for all branding and web projects.",
};

export default function DesignRequirementsLayout({ children }) {
  return <>{children}</>;
}
