export const metadata = {
  title: "Refer a Client | Aone",
  description: "Refer a client to Aone and earn rewards. Fill out our referral form.",
};

export default function ReferralLayout({ children }) {
  return <>{children}</>;
}