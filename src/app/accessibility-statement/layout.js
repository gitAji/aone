export const metadata = {
  title: "Accessibility Statement | Aone",
  description: "Aone's commitment to digital accessibility. We strive to make our AI-native solutions inclusive and accessible to all users.",
};

export default function AccessibilityLayout({ children }) {
  return <>{children}</>;
}
